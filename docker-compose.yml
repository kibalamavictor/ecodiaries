services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ecodiaries
    ports:
      - '5432:5432'
    volumes:
      - ecodiaries_pgdata:/var/lib/postgresql/data

volumes:
  ecodiaries_pgdata:
